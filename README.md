For this program:
1. Install and run blazegraph.
2. Create a config.py file with a grp_endpoint string where the grp_endpoint string is the localhost location of blazegraph (i.e: grp_endpoint = "http://127.0.0.1:9999/bigdata/namespace/kb/sparql").


Clause AI was used as a tool to support the initial creation of the code and to support with bugfixes

